# Nombre de la tienda en la factura impresa (centrado debajo de la fecha)
VITE_STORE_NAME="Nari Universe"
# API backend para produccion (si no se define usa /api, ideal con proxy local)
VITE_API_BASE_URL="http://localhost:3001/api"
