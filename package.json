{
  "name": "naripos-root",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "postinstall": "npm --prefix backend install && npm --prefix frontend install",
    "build": "node scripts/ensure-dist.js",
    "build:force": "NARIPOS_FORCE_BUILD=1 node scripts/ensure-dist.js",
    "start": "node server.js"
  }
}
